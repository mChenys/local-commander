{
  "vl_grounding": {
    "enabled": true,
    "timeout": 60,
    "max_tokens": 2048,
    "temperature": 0.1,

    "fallback_strategy": "dump_ui -> remote_vl -> local_vl",
    "_strategy_help": "降级策略（参考 ACrab）: dump_ui(ADB精确) -> remote_vl(局域网VL) -> local_vl(本地MLX)",

    "dump_ui": {
      "enabled": true,
      "priority": 1,
      "_comment": "ADB UI Dump 优先，文本元素定位精确可靠"
    },

    "remote_vl": {
      "enabled": true,
      "priority": 2,
      "base_url": "http://192.168.41.31:8000/v1",
      "model": "Qwen/Qwen3-VL-8B-Instruct",
      "api_key": "EMPTY",
      "timeout": 30,
      "_comment": "⚠️ 局域网 vLLM 服务，请修改为您的服务器地址"
    },

    "local_vl": {
      "enabled": true,
      "priority": 3,
      "model": "mlx-community/Qwen2.5-VL-7B-Instruct-4bit",
      "timeout": 180,
      "_comment": "本地 MLX VL 模型（4bit 量化版），内存占用小，速度快"
    }
  },

  "_help": {
    "说明": "Visual Grounding 服务配置 - 参考 ACrab 优化降级策略",
    "降级策略": "dump_ui -> remote_vl -> local_vl",
    "dump_ui": "ADB UI Dump 优先，文本元素定位精确",
    "remote_vl": "局域网 vLLM 服务，速度快，坐标定位准确",
    "local_vl": "本地 MLX 模型，无需网络，适合离线场景",
    "用途区分": {
      "坐标定位": "dump_ui -> remote_vl -> local_vl（按精度降级）",
      "看截图内容": "优先 local_vl（省 token，离线可用）或 remote_vl（速度更快）"
    }
  }
}
